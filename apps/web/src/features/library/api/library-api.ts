import { createClient, getVideo, listVideos } from '@reup-dubbing-studio/api-client';
import { z } from 'zod';
import { getSafeRequestId } from '@/shared/api/problem-details';
import { readRuntimeConfig } from '@/shared/config/runtime-config';
const uuid = z.string().uuid();
const video = z.object({ id: uuid, version: z.number(), displayTitle: z.string().nullable(), status: z.string(), sourceLanguage: z.string(), targetLanguage: z.string(), source: z.object({ platform: z.string(), externalId: z.string(), canonicalUrl: z.string().nullable(), durationMs: z.number().nullable(), creatorName: z.string().nullable() }), profile: z.object({ channelProfileId: uuid, channelProfileName: z.string(), seriesProfileId: uuid.nullable(), seriesProfileName: z.string().nullable() }), latestJob: z.unknown().nullable(), reviewStatus: z.string(), outputSummary: z.object({ readiness: z.enum(['NONE','PARTIAL','READY','CLEANED']), requiredVariants: z.array(z.string()), availableVariants: z.array(z.string()), warnings: z.array(z.string()) }), thumbnail: z.unknown().nullable(), outputs: z.array(z.unknown()), assets: z.array(z.unknown()), createdAt: z.string().datetime(), updatedAt: z.string().datetime(), ingestedAt: z.string().datetime().nullable(), archivedAt: z.string().datetime().nullable(), capabilities: z.object({ canOpenStudio: z.boolean(), canOpenPublishing: z.boolean(), canArchive: z.boolean() }) });
const envelope = <T extends z.ZodTypeAny>(schema: T) => z.object({ data: schema });
export type LibraryFilters = { cursor?: string; limit?: number; status?: string; platform?: string; outputReadiness?: string; query?: string; includeArchived?: boolean };
export type LibraryVideo = z.infer<typeof video>;
export class LibraryApiError extends Error { override readonly name = 'LibraryApiError'; constructor(message: string, readonly code?: string, readonly requestId?: string) { super(message); } }
function client() { return createClient({ baseUrl: readRuntimeConfig().controlPlaneUrl }); }
export async function fetchVideos(filters: LibraryFilters, signal?: AbortSignal) { const result = await listVideos({ client: client(), query: filters as never, signal: signal ?? null }); if (result.error) throw parseError(result.error, result.response); const parsed = envelope(z.object({ items: z.array(video), nextCursor: z.string().nullable() })).safeParse(result.data); if (!parsed.success) throw new LibraryApiError('Control Plane trả về dữ liệu Library không hợp lệ.'); return parsed.data.data; }
export async function fetchVideo(id: string, signal?: AbortSignal) { const result = await getVideo({ client: client(), path: { videoId: id }, signal: signal ?? null }); if (result.error) throw parseError(result.error, result.response); const parsed = envelope(video).safeParse(result.data); if (!parsed.success) throw new LibraryApiError('Control Plane trả về chi tiết video không hợp lệ.'); return parsed.data.data; }
function parseError(value: unknown, response: Response) { const problem = value as { code?: unknown; detail?: unknown }; return new LibraryApiError(typeof problem.detail === 'string' ? problem.detail : 'Không thể tải thư viện video.', typeof problem.code === 'string' ? problem.code : undefined, getSafeRequestId(value, response.headers.get('X-Request-Id'))); }
