`errors/` contains HTTP-independent domain errors. `ProblemDetailsFilter` is the
HTTP boundary that maps those errors, validation failures, and known persistence
errors to the Problem Details shape from the API contract.
