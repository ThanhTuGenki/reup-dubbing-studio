pre-commit:
  parallel: true
  commands:
    lint-staged:
      run: pnpm exec lint-staged

pre-push:
  commands:
    typecheck:
      run: pnpm typecheck

commit-msg:
  commands:
    commitlint:
      run: pnpm exec commitlint --edit {1}
